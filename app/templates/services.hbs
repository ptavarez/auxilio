<h2>Services</h2>

{{#each model as |service|}}
  <ul>
    <li>
      {{#link-to 'service' service}}
        {{service.name}}
      {{/link-to}}
    </li>
    <li>
      {{request-service request='requestAssist' service=service}}
    </li>
  </ul>
{{/each}}
